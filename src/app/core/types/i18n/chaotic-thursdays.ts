export type SeoCopy = {
  title: string;
  description: string;
};

export type HeroCopy = {
  badge: string;
  title: string;
  subtitleLead: string;
  subtitleStrong: string;
  upcomingSessions: string;
};

export type HeroInfoCopy = {
  title: string;
  cityLabel: string;
  cityMissing: string;
  placeLabel: string;
  placeMissing: string;
  addressLabel: string;
  addressMissing: string;
  scheduleLabel: string;
  scheduleMissing: string;
  priceLabel: string;
  priceMissing: string;
};

export type EditionSelectorCopy = {
  label: string;
  ariaLabel: string;
};

export type StateCopy = {
  noOccurrences: string;
};

export type ErrorCopy = {
  coreNotFound: string;
  noEditions: string;
  invalidDefault: string;
  groupedRpc: string;
  occurrences: string;
  program: string;
  gmProfile: string;
};

export type HeroCtaCopy = {
  primaryLabel: string;
  hostSignupLabel: string;
};

export type AboutCopy = {
  title: string;
  text: string;
};

export type SectionCopy = {
  title: string;
  subtitle: string;
};

export type StandardsCopy = {
  title: string;
  subtitle: string;
  expectationsTitle: string;
};

export type CardCopy = {
  id: number;
  title: string;
  text: string;
};

export type StepCopy = {
  id: number;
  time: string;
  title: string;
  text: string;
};

export type FaqCopy = {
  id: number;
  h: string;
  a: string;
};
